# AGENTS.md

## プロジェクト概要

このプロジェクトは、STEPS Musical Company の紙アンケートを
スマートフォンでスキャンし、回答内容を読み取って
既存Googleフォームへ手動転記するためのコピー支援Webアプリです。

詳細な製品仕様は `docs/MVP_SPEC.md` を参照してください。

---

## 開発方針

- 変更を行う前に、関連する既存コードと `docs/MVP_SPEC.md` を確認すること。
- 一度に必要以上の機能を実装しないこと。
- 現在指定されているPhaseの範囲を優先すること。
- 既存の動作を壊さないよう、小さな単位で変更すること。
- 不要なライブラリやサービスを追加しないこと。
- アンケートの項目名、選択肢、意味を勝手に変更しないこと。
- 固定レイアウトのアンケートであることを活用すること。
- チェックボックス判定は原則としてOpenCVによる画像処理を使用すること。
- 手書き文字についてのみOCR APIを使用する方針とすること。

---

## 個人情報・セキュリティ

このプロジェクトでは、氏名、住所、郵便番号、メールアドレス、
自由記述などの個人情報を扱います。

以下を必ず守ってください。

- アップロードされたアンケート画像を永続保存しない。
- 画像は可能な限りメモリ上で処理する。
- 一時ファイルを作成した場合は処理終了後に削除する。
- 個人情報をログに出力しない。
- OCR結果の本文をログに出力しない。
- HTTP request bodyをログに出力しない。
- Base64画像データをログに出力しない。
- APIキー、秘密鍵、Service Account情報をコードに直接書かない。
- 秘密情報は環境変数またはSecret Managerから取得する。
- 秘密情報をGitへコミットしない。
- `test-data/private/` の内容をGitへコミットしない。
- 外部サービスとの通信にはHTTPS/TLSを使用する。
- APIキーやGoogleの認証情報をフロントエンドへ送信しない。

---

## Git

- `.gitignore` を尊重すること。
- `test-data/private/` 内のファイルをGit管理対象にしないこと。
- `.env`、秘密鍵、Google Service Accountの認証情報をGit管理対象にしないこと。
- ユーザーから明示的な指示がない限り、既存のGit履歴を破壊する操作を行わないこと。
- `git reset --hard`、force pushなどの破壊的操作を勝手に行わないこと。

---

## アンケート画像

基準となる未記入アンケート:

`reference/questionnaire_blank.pdf`

記入済みテスト画像:

`test-data/private/`

記入済みテスト画像には実際の個人情報が含まれる可能性があるため、
内容をログ、README、テスト結果等へコピーしないこと。

---

## 認識処理

認識結果が不確かな場合、推測して確定しないこと。

可能な範囲の認識結果を返し、

`needsReview = true`

として人間による確認を求めること。

特に以下は慎重に扱うこと。

- 氏名
- 郵便番号
- 住所
- メールアドレス
- 自由記述
- 「その他」の記述
- 関係者名

メールアドレス等をOCR結果から勝手に補正しないこと。

---

## テスト

- 実装した処理について可能な範囲でテストを追加すること。
- 個人情報をテストコードへハードコードしないこと。
- テスト用の架空データが必要な場合は、明らかなダミーデータを使用すること。
- 変更後は関連テストを実行すること。
- テストが失敗した状態で完了扱いにしないこと。
- 実行できなかったテストがある場合は、その理由を報告すること。

---

## Codexの作業報告

大きな作業を完了した際は、以下を簡潔に報告してください。

1. 何を変更したか
2. 変更した主なファイル
3. 実行したテスト
4. 現在の制限事項
5. 次に推奨する作業

ユーザーが確認できるよう、専門用語だけで説明せず、
必要に応じて日本語で簡潔に説明してください。
